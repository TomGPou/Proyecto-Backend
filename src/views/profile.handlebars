<style>
  body { background-color: #333; } .a2 { color: #333; } .a2:hover { color: #000;
  }
</style>

<h2>Usuario: {{user.first_name}}, {{user.last_name}}</h2>
<h2>Email: {{user.email}}</h2>
{{#if user.isAdmin}}
  <h2>Administrador</h2>
{{/if}}

<form action="/api/auth/logout" method="get">
  <button type="submit" class="btn btn-success">Cerrar sesión</button>
</form>